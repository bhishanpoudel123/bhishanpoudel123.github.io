{
  "id": 4,
  "category": "SQL",
  "question": "You want to calculate the division between total and quantity as a decimal number (both fields being stored as integers). What should you do?",
  "answers": [
    "Cast either [total] or [quantity] into a decimal number"
  ],
  "metadata": {
    "language": "SQL",
    "type": "MCQ",
    "answer_format": "code",
    "difficulty": "medium"
  },
  "options": [
    "Cast [total / quantity] into a decimal number",
    "Cast either [total] or [quantity] into a decimal number",
    "Change the default display settings of your database",
    "Use the following query: SELECT total / quantity FROM product"
  ]
}